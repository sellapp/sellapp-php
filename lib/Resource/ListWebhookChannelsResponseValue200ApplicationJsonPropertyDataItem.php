<?php

declare(strict_types=1);

// This file is auto-generated by oagen. Do not edit.

namespace SellApp\Resource;

/** A seller-configured outbound webhook destination. IDs are stable UUIDs and are not reused when another channel is deleted. */
readonly class ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem implements \JsonSerializable
{
    use JsonSerializableTrait;

    private const SCHEMA = ['name' => 'ListWebhookChannelsResponseValue200ApplicationJsonPropertyDataItem', 'required' => ['id', 'name', 'url', 'allowed_notifications', 'signing_secret_configured'], 'properties' => ['id' => ['kind' => 'primitive', 'type' => 'string', 'format' => 'uuid'], 'name' => ['kind' => 'nullable', 'inner' => ['kind' => 'primitive', 'type' => 'string']], 'url' => ['kind' => 'primitive', 'type' => 'string', 'format' => 'uri'], 'allowed_notifications' => ['kind' => 'array', 'items' => ['kind' => 'enum', 'values' => ['booking.cancelled', 'booking.rescheduled', 'cash_app.payment_requires_attention', 'charge.completed', 'charge.created', 'charge.disputed', 'charge.partial_paid', 'charge.review', 'feedback.created', 'order.completed', 'order.created', 'order.disputed', 'order.manual_payment_completed', 'order.paid', 'order.partial_paid', 'product.created', 'product.trashed', 'product.updated', 'subscription.cancelled', 'subscription.created', 'subscription.renewed', 'ticket_message.created', 'variant.created', 'variant.sold_out', 'variant.updated']], 'uniqueItems' => true], 'signing_secret_configured' => ['kind' => 'primitive', 'type' => 'boolean']], 'additionalProperties' => true];

    public function __construct(
        /** Stable identifier for this webhook channel. */
        public string $id,
        /** Optional seller-facing reference name. */
        public ?string $name,
        /** Public HTTP or HTTPS destination. Local, private, reserved, link-local, metadata, credential-bearing, and Discord webhook URLs are rejected. Delivery resolves and pins a public address and does not follow redirects. */
        public string $url,
        /**
         * Complete set of public webhook event names enabled for this destination. Updating this field replaces the previous filter.
         * @var array<\SellApp\Resource\Event>
         */
        public array $allowedNotifications,
        /** Whether the store has an outbound webhook signing secret. The stored secret is never returned. */
        public bool $signingSecretConfigured,
        /** @var array<string, mixed> */
        public array $additionalProperties = [],
        private array $presentFields = [],
    ) {
    }

    public static function fromArray(array $data): self
    {
        self::validate($data, 'response');
        return new self(
            id: $data['id'],
            name: $data['name'] ?? null,
            url: $data['url'],
            allowedNotifications: array_map(fn ($item) => Event::tryFrom($item) ?? $item, $data['allowed_notifications']),
            signingSecretConfigured: $data['signing_secret_configured'],
            additionalProperties: SchemaValidator::unknownFields($data, ['id', 'name', 'url', 'allowed_notifications', 'signing_secret_configured']),
            presentFields: array_keys($data),
        );
    }

    public static function validate(array $data, string $direction = 'request'): void
    {
        SchemaValidator::validateModel($data, self::SCHEMA, $direction);
    }

    public function toArray(): array
    {
        return array_replace($this->additionalProperties, SchemaValidator::presentFields([
            'id' => $this->id,
            'name' => $this->name,
            'url' => $this->url,
            'allowed_notifications' => array_map(fn ($item) => $item instanceof \BackedEnum ? $item->value : $item, $this->allowedNotifications),
            'signing_secret_configured' => $this->signingSecretConfigured,
        ], ['id', 'name', 'url', 'allowed_notifications', 'signing_secret_configured'], $this->presentFields));
    }
}
